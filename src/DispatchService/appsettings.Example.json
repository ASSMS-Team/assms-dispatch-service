{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "default": "Server=localhost;Port=3306;Database=dispatchdb;User ID=<your-mysql-user>;Password=<your-mysql-password>;"
  },
  "Kafka": {
    "BootstrapServers": "localhost:9092"
  },
  "CandidateMatching": {
    "//": "All five serviceCategory values JobCreated can carry must appear here. A category with no mapping resolves to UnmappedServiceCategory, and Dispatch quarantines the event - the job is never evaluated and never assigned. Each value must match a technician_skills.skill string exactly; the lookup is case-insensitive on the category, not on the skill.",
    "RequiredSkillByServiceCategory": {
      "INSTALLATION": "AC_INSTALLATION",
      "REPAIR": "AC",
      "MAINTENANCE": "AC_MAINTENANCE",
      "INSPECTION": "AC_INSPECTION",
      "WARRANTY_CLAIM": "AC_WARRANTY"
    }
  },
  "Authentication": {
    "Jwt": {
      "Issuer": "assms-customer-asset-service",
      "Audience": "assms-internal",
      "SigningKey": "<a-secret-value-of-at-least-32-bytes>"
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5173"
    ]
  }
}
